pub mod math;


fn suma(ma:Matrix, mb:Matrix) {
    if ma.m == mb.m { 
        if ma.n == mb.n {
        suma(ma,mb)}
        } 
    else {println!("MATH ERROR")}}
fn suma(ma:Matrix, mb:Matrix){
    for i in n {
        for j in m {
            let matrixc=matrixa(j.i)+matrixb(j.i)}}
    println!{ matrixc}
}



fn mul(a: u64, m: Matrix) -> Matrix {
    
}


fn mul(m1: Matrix, m2: Matrix) -> Matrix {
    if (m1.n != m2.m)

    for i in 0..rows {
        for j in 0..cols {
           
        }
    }
}